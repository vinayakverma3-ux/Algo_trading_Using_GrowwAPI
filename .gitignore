# secrets — never commit
.env

# environment
.venv/
__pycache__/
*.pyc

# runtime state: trade logs, paper positions, heartbeats, session output
logs/

# downloaded price history (regenerated by momentum/backtest.py --refresh)
momentum/*.parquet

# machine-specific MCP paths
.mcp.json

# runtime state
momentum/live_state.json
